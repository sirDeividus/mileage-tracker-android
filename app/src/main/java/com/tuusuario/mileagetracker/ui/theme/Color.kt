package com.tuusuario.mileagetracker.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Color.kt
 * -----------------------------------------------------------------------
 * Paleta de colores central de la app, igual que hicimos con colors.js
 * en la versión React Native. Mantener todos los colores aquí evita
 * "colores mágicos" repetidos por toda la app.
 * -----------------------------------------------------------------------
 */

// Color principal de marca (verde "dinero / finanzas")
val PrimaryGreen = Color(0xFF2E7D32)
val PrimaryGreenDark = Color(0xFF1B5E20)
val PrimaryGreenLight = Color(0xFF66BB6A)

// Color de acento para "Stop Work" / alertas suaves
val AccentOrange = Color(0xFFFF8F00)

// Estados
val SuccessGreen = Color(0xFF2E7D32)
val DangerRed = Color(0xFFD32F2F)
val WarningYellow = Color(0xFFF9A825)

// Fondos y superficies
val BackgroundLight = Color(0xFFF5F7F6)
val SurfaceWhite = Color(0xFFFFFFFF)
val SurfaceAlt = Color(0xFFEEF3F1)

// Textos
val TextPrimary = Color(0xFF1A1E1B)
val TextSecondary = Color(0xFF5F6B63)
val TextMuted = Color(0xFF9AA79F)
val TextOnPrimary = Color(0xFFFFFFFF)

// Bordes
val BorderColor = Color(0xFFE0E6E2)
